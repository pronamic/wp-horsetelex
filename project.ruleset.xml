<?xml version="1.0"?>
<ruleset name="Horsetelex">
	<description>A custom set of rules to check for the Horsetelex WordPress plugin.</description>

	<exclude-pattern>/node_modules</exclude-pattern>

	<rule ref="WordPress">
		<exclude name="WordPress.WhiteSpace.ScopeIndent" />
	</rule>
</ruleset>
